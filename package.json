{
  "name": "long-distance-love-room",
  "version": "1.0.0",
  "description": "A private two-person long-distance room web app.",
  "license": "MIT",
  "keywords": [
    "express",
    "socket.io",
    "sqlite",
    "long-distance",
    "couple",
    "self-hosted"
  ],
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/config.js && node --check src/db.js && node --check src/passcodes.js && node --check src/uploads.js && node --check src/server.js && node --check public/app.js && node --check scripts/init-db.js",
    "init-db": "node scripts/init-db.js",
    "docker:up": "docker compose up -d --build",
    "docker:logs": "docker compose logs -f",
    "docker:down": "docker compose down"
  },
  "engines": {
    "node": ">=20 <25"
  },
  "dependencies": {
    "better-sqlite3": "8.7.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "multer": "^2.0.2",
    "socket.io": "^4.8.1"
  }
}
